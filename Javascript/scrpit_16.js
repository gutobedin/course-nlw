// Manipulando Arrays

// Tranformar uma cadeia de caracteres el elementos de um array


let word = "manipulação"
console.log(Array.from(word)) // com o array.from cada letra vira uma posicão, vira um array