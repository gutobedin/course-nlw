// Operadores lógicos (logical operators)

// - 2 valores boleanos, quando verificados,
// resultará em verdadeiro ou falso

let pao = true
let queijo = true 

// AND &&
console.log(pao && queijo)
// OR ||
console.log (pao || quejo)
// NOT !
console.log(!pao)